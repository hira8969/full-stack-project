server:
  port: 8080

spring:
  application:
    name: bizflow-backend
  datasource:
    url: jdbc:h2:mem:bizflow;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: root
    password:Hira2006@
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
  h2:
    console:
      enabled: true

bizflow:
  api-prefix: /api/v1
