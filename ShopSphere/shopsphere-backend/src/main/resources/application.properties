# Server Configuration
server.port=${PORT:8080}
server.servlet.context-path=/

# Spring Profile
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# Application Information
spring.application.name=shopsphere-backend

# Security & JWT Configuration
shopsphere.app.jwt-secret=${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
shopsphere.app.jwt-expiration-ms=${JWT_EXPIRATION_MS:86400000}

# File Upload Storage Path
shopsphere.app.upload-dir=${UPLOAD_DIR:./uploads}

# CORS Configuration
shopsphere.app.cors.allowed-origins=${ALLOWED_ORIGINS:http://localhost:4200,http://127.0.0.1:4200}
